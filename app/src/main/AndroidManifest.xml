<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="practica.univalle.basicretrofitadapter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BasicRetrofitAdapter">
        <activity
            android:name=".PrincipalActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Example2.BCMainActivity"
            android:exported="false" />
        <activity
            android:name=".Example1.MainActivity"
            android:exported="true">

        </activity>

        <receiver
            android:name=".Example1.MyReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="practica.univalle.basicretrofitadapter.CUSTOM_ACTION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>